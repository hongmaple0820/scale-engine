name: Publish npm package

on:
  push:
    tags:
      - 'v*'

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          registry-url: https://registry.npmjs.org
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate Gitee release sync secret
        if: github.repository == 'hongmaple0820/scale-engine'
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          if [ -z "$GITEE_TOKEN" ]; then
            echo "::error::GITEE_TOKEN is required for official releases so Gitee releases cannot silently drift from GitHub/npm."
            exit 1
          fi

      - name: Verify
        run: |
          npm run learning:health
          npm run docs:health
          npm run lint
          npm run typecheck
          npm run build
          npm test

      - name: Pack dry run
        run: npm pack --dry-run

      - name: Publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public --provenance

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=$(node -p "require('./package.json').version")
          # Extract changelog section for this version
          NOTES=$(awk "/^## ${VERSION} -/{found=1; next} /^## [0-9]/{if(found) exit} found{print}" CHANGELOG.md)
          gh release create "v${VERSION}" \
            --title "v${VERSION}" \
            --notes "${NOTES:-Release v${VERSION}}" \
            --generate-notes

      - name: Sync releases to Gitee
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
        run: |
          npm run release:sync-gitee -- --gitee-owner hongmaple --gitee-repo scale-engine
