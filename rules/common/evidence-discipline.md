---
paths:
  - "**/*"
---
# Evidence Discipline (证据纪律)

汇报进度前，先把每个声明与本轮工具结果对齐。这是 G23 测试完整性门的 prompt 层前置预防：
事后检测（G23）+ 事前纪律（本规则）形成双层防线。

## 核心规则

1. **只汇报有证据支持的工作。** 未验证的内容明确标注为未验证（如 `[UNCERTAIN]`），不要默认它成立。
2. **失败就报告失败。** 测试失败贴失败输出；步骤被跳过就说明跳过，不要掩盖或粉饰。
3. **完成后再说完成。** 完成并验证后再直接说"完成"，不要使用"应该可以""大概没问题"之类模糊措辞。
4. **没有验证结果不说"已通过"。** `dry-run` 只代表入口可调度，不代表质量通过。

## 边界：判断 vs 行动

- 当用户只是描述问题、提问或思考，而非要求修改时，交付物是你的判断——报告发现后停止，不要主动修复。
- 运行会改变系统状态的命令前，先确认证据确实支持这个具体动作。

> 单一事实源：本规则正文与 `src/agents/evidenceDiscipline.ts` 的 `EVIDENCE_DISCIPLINE_PROMPT`
> 保持一致；后者被 12 个 Agent Profile 默认继承，并在 Cortex `SessionStart` 注入。
